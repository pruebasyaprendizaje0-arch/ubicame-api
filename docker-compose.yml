version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ubicame-api
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - PORT=3000
      - HOST=0.0.0.0
      - DATABASE_URL=postgresql://ubicame_user:ubicame_pass@postgres:5432/ubicame_core?schema=public
      - JWT_SECRET=dev-secret-key-change-in-production-12345
      - CORS_ORIGINS=https://api.ubicame.cc,https://menuqr.ubicame.cc,https://misreservaciones.ubicame.cc,http://localhost:3000,http://localhost:5173
    depends_on:
      postgres:
        condition: service_healthy

  postgres:
    image: postgres:16-alpine
    container_name: ubicame-postgres
    restart: always
    environment:
      POSTGRES_USER: ubicame_user
      POSTGRES_PASSWORD: ubicame_pass
      POSTGRES_DB: ubicame_core
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ubicame_user -d ubicame_core"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data: